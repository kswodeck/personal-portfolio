# personal-portfolio
A personal portfolio built four times — once in each major front-end framework — all reading from a single shared `shared/content.json`. Each implementation is a fully independent, standalone build served at its own URL path.
